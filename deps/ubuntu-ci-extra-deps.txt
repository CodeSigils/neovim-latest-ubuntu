# Extra packages required by the CI/container image beyond the manual host build list.
# These support HTTPS fetches, packaging verification, and test.sh execution.
ca-certificates
file
lintian
lua5.1
sudo
